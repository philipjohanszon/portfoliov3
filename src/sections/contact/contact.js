import "./contact.scoped.css";
import PageBookmarkOffset from "../../components/pageBookmarkOffset/pageBookmarkOffset";

const Contact = () => (
  <section className="container">
    <PageBookmarkOffset idField="contact" />
    <h1>Contact</h1>
  </section>
);

export default Contact;

